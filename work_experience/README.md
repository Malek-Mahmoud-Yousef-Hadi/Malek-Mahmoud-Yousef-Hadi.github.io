# Professional Experience Evidence

Place the two evidence files in this folder with the exact filenames below. The portfolio displays them through the `[ VIEW_WORK ]` buttons in the `PROFESSIONAL_EXPERIENCE` section.

- `full-web-penetration-testing-real-website.pdf`
- `black-box-internal-network-penetration-testing.pdf`

If the files use different names or formats, update the matching `file` and `format` values in `assets/js/config.js`.
